# --- Core Framework ---
Flask==3.0.3              # Lightweight web framework for APIs
# or use FastAPI if you prefer async:
# FastAPI==0.115.2
# uvicorn==0.30.5          # ASGI server for FastAPI apps

# --- Environment & Configuration ---
python-dotenv==1.0.1       # Load environment variables from .env files

# --- Database & ORM ---
SQLAlchemy==2.0.36         # ORM for database interaction
pymysql==1.1.0             # MySQL database driver
psycopg2-binary==2.9.9     # PostgreSQL driver (use one depending on your DB)
# or for MongoDB:
# pymongo==4.9.1

# --- Authentication & Security ---
bcrypt==4.1.3              # Password hashing
PyJWT==2.9.0               # JSON Web Token authentication

# --- Utilities ---
requests==2.32.3           # Handle external HTTP requests
email-validator==2.2.0     # Validate emails in user input
passlib==1.7.4             # Password hashing and security helpers
python-dateutil==2.9.0.post0  # Date and time parsing helpers

# --- Error Handling & Logging ---
loguru==0.7.2              # Simplified and elegant logging library

# --- Development Tools ---
black==24.10.0             # Code formatter
flake8==7.1.1              # Linter for clean code
pytest==8.3.3              # Testing framework
coverage==7.6.1            # Code coverage for tests

# --- Deployment ---
gunicorn==23.0.0           # WSGI server for Flask (for production)
# or for FastAPI:
# uvicorn[standard]==0.30.5
